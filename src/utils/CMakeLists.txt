file(GLOB_RECURSE UTILS_LIB_SOURCES ${PROJECT_SOURCE_DIR}/src/utils/*.cpp)
file(GLOB_RECURSE UTILS_LIB_HEADERS ${PROJECT_SOURCE_DIR}/src/utils/*.h)

# Create utils
add_library(utils STATIC ${UTILS_LIB_SOURCES} ${UTILS_LIB_HEADERS})
set_target_properties(utils PROPERTIES LINKER_LANGUAGE CXX)
